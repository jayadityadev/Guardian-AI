import React from 'react'

const Upload = () => {
    return (
        <div className="glass-card rounded-2xl p-6">
            <h2 className="font-headline-md mb-2">Upload (placeholder)</h2>
            <p className="text-sm text-on-surface-variant">Upload UI not implemented here — reserved for later. Use `/upload` to test routing.</p>
        </div>
    )
}

export default Upload
